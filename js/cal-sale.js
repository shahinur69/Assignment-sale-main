function calKitchenWork(){
    const find = document.getElementById('input-field');
    console.log(find);
    const field = getInputValue('input-field');
    console.log(field);
    return field, find;
}